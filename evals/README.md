# Evaluation contract

Retrieval questions must include the expected answer and original source evidence, independently of chunk boundaries. Keep development questions separate from the held-out test set. Answerable retrieval questions and unanswerable rejection questions have separate denominators. Generated tests and fixtures must be labeled; they do not establish real-world model accuracy.

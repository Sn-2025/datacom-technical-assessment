# Configuration

Non-secret, reproducible corpus and evaluation manifests belong here. Connection secrets belong in environment variables, mounted secret files or a server-side session, never in committed configuration.
